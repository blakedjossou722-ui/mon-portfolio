<section id="hero" class="site-hero">
  <div class="site-hero-content">
    <div class="space-y-6 max-w-3xl">
      <div class="inline-flex items-center gap-2 rounded-full bg-slate-900/70 border border-slate-700 px-4 py-2 text-sm text-slate-300">
        <span class="h-2 w-2 rounded-full bg-emerald-400"></span>
        <span>Étudiant en 3ᵉ année • Développeur Web & Designer</span>
      </div>
      <h1 class="text-5xl md:text-6xl font-extrabold tracking-tight text-white leading-tight">
        Je conçois des sites <span class="text-gradient">modernes</span> & des visuels <span class="text-gradient">impactants</span>.
      </h1>
      <p class="text-slate-400 max-w-2xl leading-relaxed text-lg">
        Combinaison unique entre la logique du développement web et la créativité du design graphique. Je transforme des idées en expériences numériques performantes, accessibles et élégantes.
      </p>

      <div class="flex flex-wrap gap-4">
        <a href="#projects" class="btn-primary inline-flex items-center gap-2">
          <span>Voir mes projets</span>
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3" />
          </svg>
        </a>
        <a href="#contact" class="btn-secondary inline-flex items-center gap-2">
          <span>Me contacter</span>
        </a>
      </div>
    </div>

    <div class="site-hero-panel glass-panel">
      <div class="panel-header">
        <span class="badge">Services</span>
        <p class="text-slate-400 text-sm">UI/UX, intégration et optimisation de performances.</p>
      </div>
      <div class="panel-list">
        <div class="panel-item">
          <h3 class="text-white font-semibold">Design & Branding</h3>
          <p class="text-slate-400 text-sm">Maquettes interactives, identité visuelle et supports digitaux.</p>
        </div>
        <div class="panel-item">
          <h3 class="text-white font-semibold">Sites Web</h3>
          <p class="text-slate-400 text-sm">Sites responsives, animations modernes et SEO friendly.</p>
        </div>
        <div class="panel-item">
          <h3 class="text-white font-semibold">Accessibilité</h3>
          <p class="text-slate-400 text-sm">Normes WCAG, code propre et expériences inclusives.</p>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  (function() {
    const nodes = Array.from(document.querySelectorAll('.site-hero .text-gradient'));
    nodes.forEach((node, index) => {
      node.style.transition = 'color 0.35s ease';
      node.style.transitionDelay = `${index * 60}ms`;
      node.classList.add('text-gradient-animation');
    });
  })();
</script>

<style>
  .site-hero {
    padding: 6rem 1.5rem 4rem;
    position: relative;
    overflow: hidden;
  }

  .site-hero-content {
    display: grid;
    grid-template-columns: minmax(0, 1.1fr) minmax(340px, 0.9fr);
    gap: 3rem;
    align-items: center;
    max-width: 1200px;
    margin: 0 auto;
  }

  .glass-panel {
    background: rgba(15, 23, 42, 0.72);
    border: 1px solid rgba(148, 163, 184, 0.1);
    backdrop-filter: blur(20px);
    border-radius: 2rem;
    padding: 2rem;
    box-shadow: 0 30px 80px rgba(0, 0, 0, 0.25);
  }

  .panel-header {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    margin-bottom: 1.5rem;
  }

  .badge {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.55rem 1rem;
    border-radius: 9999px;
    background: rgba(79, 70, 229, 0.15);
    color: #a5b4fc;
    font-size: 0.75rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.08em;
  }

  .panel-list {
    display: grid;
    gap: 1rem;
  }

  .panel-item {
    padding: 1rem;
    border-radius: 1.5rem;
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(148, 163, 184, 0.12);
  }

  .btn-primary {
    background: linear-gradient(135deg, #6366f1, #8b5cf6);
    color: white;
    padding: 1rem 1.5rem;
    border-radius: 9999px;
    font-weight: 700;
  }

  .btn-secondary {
    background: rgba(15, 23, 42, 0.75);
    color: #e2e8f0;
    padding: 1rem 1.5rem;
    border-radius: 9999px;
    border: 1px solid rgba(148, 163, 184, 0.16);
  }

  .text-gradient {
    background: linear-gradient(90deg, #8b5cf6, #22d3ee);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  @media (max-width: 1024px) {
    .site-hero-content {
      grid-template-columns: 1fr;
    }
  }
</style>
